# 记录领域术语

## 记录（Record）

一段从开始到完成连续计时的工作记录。记录只有“进行中”和“已完成”两种产品状态。

## 切点（Timeline Point）

记录内部的时间边界。切点从该时刻起定义后续时间段的工作类型，可用于拆分或重新归类；切点本身不停止记录。

## 关键帧（Marker）

记录中的视觉与语义标记，用于定位上下文、备注或截图关联。关键帧不改变记录时长或工作类型；当前“打关键帧”动作会同时建立一个切点，方便随后为右侧时间段归类。
